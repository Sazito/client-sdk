---
title: Wallet
description: Wallet balance, transaction history, and invoice credit operations.
---

## Methods

### Get Balance

Use: `wallet.getBalance(options?)`.

Returns: `SazitoResponse<Wallet>`.

### List Transactions

Use: `wallet.listTransactions(filters?, options?)`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `filters.page_number` | `number` | Optional | Page number (default `1`). |
| `filters.page_size` | `number` | Optional | Page size (default `100`). |
| `options` | `RequestOptions` | Optional | Per-request controls. |

Returns: `SazitoResponse<{ transactions: WalletTransaction[] }>`.

### Apply Credit

Use: `wallet.applyCredit(invoiceId, options?)`.

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `invoiceId` | `number` | Required | Invoice ID (must be positive). |
| `options` | `RequestOptions` | Optional | Per-request controls. |

Returns: `SazitoResponse<Invoice>`.

### Remove Credit

Use: `wallet.removeCredit(invoiceId, options?)`.

Same schema as `applyCredit`.

## `Wallet` fields

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `balance` | `number` | Required | Current credit balance. |
| `enabled` | `boolean` | Required | Wallet feature availability. |
| `currency` | `string` | Optional | Currency code/display text. |
| `transactions` | `WalletTransaction[]` | Optional | Inlined transactions list. |

## Known errors

| Type | When it happens |
| --- | --- |
| `validation` | Invalid invoice ID for credit operations. |
| `api` | Unauthorized wallet/invoice operation. |

## Examples

<Tabs items={["Next.js (Client)", "Next.js (Server)", "React", "Vue", "Nuxt", "Nuxt.js (Server)"]}>

<Tab value="Next.js (Client)">

```tsx
// app/lib/sazito-client.ts
import { createSazitoClient } from '@sazito/client-sdk';

export const sazito = createSazitoClient({
  domain: process.env.NEXT_PUBLIC_SAZITO_DOMAIN!,
});
```

```tsx
// app/[lang]/(home)/page.tsx
'use client';
import { useEffect, useState } from 'react';
import { sazito } from '@/app/lib/sazito-client';

export default function Example() {
  const [data, setData] = useState<any>(null);

  useEffect(() => {
    sazito.wallet.getBalance().then((res) => setData(res.data ?? null));
  }, []);

  return <pre>{JSON.stringify(data, null, 2)}</pre>;
}
```

</Tab>

<Tab value="Next.js (Server)">

```tsx
// app/lib/sazito-server.ts
import { createSazitoClient } from '@sazito/client-sdk';

export function getSazitoServerClient() {
  return createSazitoClient({ domain: process.env.SAZITO_STORE_DOMAIN! });
}
```

```tsx
// app/actions/example.ts
'use server';
import { getSazitoServerClient } from '@/app/lib/sazito-server';

export async function runExample() {
  const client = getSazitoServerClient();
  const res = await client.wallet.getBalance();

  return res.data;
}
```

</Tab>

<Tab value="React">

```tsx
// src/lib/sazito.ts
import { createSazitoClient } from '@sazito/client-sdk';

export const sazito = createSazitoClient({
  domain: import.meta.env.VITE_SAZITO_DOMAIN,
});
```

```tsx
// src/components/Example.tsx
import { useEffect, useState } from 'react';
import { sazito } from '../lib/sazito';

export function Example() {
  const [data, setData] = useState<any>(null);

  useEffect(() => {
    sazito.wallet.getBalance().then((res) => setData(res.data ?? null));
  }, []);

  return <pre>{JSON.stringify(data, null, 2)}</pre>;
}
```

</Tab>

<Tab value="Vue">

```vue
<!-- src/lib/sazito.ts -->
<script lang="ts">
import { createSazitoClient } from '@sazito/client-sdk';

export const sazito = createSazitoClient({
  domain: import.meta.env.VITE_SAZITO_DOMAIN,
});
</script>
```

```vue
<!-- src/components/Example.vue -->
<script setup lang="ts">
import { ref, onMounted } from 'vue';
import { sazito } from '../lib/sazito';

const data = ref<any>(null);

onMounted(async () => {
  const res = await sazito.wallet.getBalance();
  data.value = res.data ?? null;
});
</script>

<template>
  <pre>{{ JSON.stringify(data, null, 2) }}</pre>
</template>
```

</Tab>

<Tab value="Nuxt">

```vue
<!-- plugins/sazito.client.ts -->
<script lang="ts">
import { createSazitoClient } from '@sazito/client-sdk';

export default defineNuxtPlugin(() => {
  const config = useRuntimeConfig();
  const sazito = createSazitoClient({ domain: config.public.sazitoDomain });
  return { provide: { sazito } };
});
</script>
```

```vue
<!-- pages/example.vue -->
<script setup lang="ts">
import { ref, onMounted } from 'vue';
const { $sazito } = useNuxtApp();

const data = ref<any>(null);

onMounted(async () => {
  const res = await $sazito.wallet.getBalance();
  data.value = res.data ?? null;
});
</script>

<template>
  <pre>{{ JSON.stringify(data, null, 2) }}</pre>
</template>
```

</Tab>

<Tab value="Nuxt.js (Server)">

```ts
// server/utils/sazito.ts
import { createSazitoClient } from '@sazito/client-sdk';

export function getSazitoClient() {
  const config = useRuntimeConfig();
  return createSazitoClient({ domain: config.public.sazitoDomain });
}
```

```ts
// server/api/example.get.ts
import { getSazitoClient } from '../utils/sazito';

export default defineEventHandler(async () => {
  const client = getSazitoClient();
  const res = await client.wallet.getBalance();

  return res.data;
});
```

</Tab>

</Tabs>

{/* AUTO_FIELDS_START */}
## Type Details

The following tables are generated from SDK TypeScript types and include nested object fields.

#### TransactionFilters

Source: `src/api/wallet.ts`

| Field | Type | Required |
| --- | --- | --- |
| `page_number` | `number` | Optional |
| `page_size` | `number` | Optional |

#### WalletTransactionsResponse

Source: `src/api/wallet.ts`

| Field | Type | Required |
| --- | --- | --- |
| `transactions` | `WalletTransaction[]` | Required |

#### Wallet

Source: `src/api/wallet.ts`

| Field | Type | Required |
| --- | --- | --- |
| `balance` | `number` | Required |
| `enabled` | `boolean` | Required |
| `currency` | `string` | Optional |
| `transactions` | `WalletTransaction[]` | Optional |

#### WalletTransaction

Source: `src/api/wallet.ts`

| Field | Type | Required |
| --- | --- | --- |
| `id` | `string \| number` | Required |
| `reason` | `WalletTransactionReason` | Required |
| `amount` | `number` | Required |
| `createdAt` | `string` | Required |
| `metaData` | `Record<string, unknown>` | Optional |

#### WalletBalance

Source: `src/api/wallet.ts`

| Field | Type | Required |
| --- | --- | --- |
| `balance` | `number` | Required |
| `enabled` | `boolean` | Required |

#### WalletTransactionReason

Source: `src/api/wallet.ts`

Type: ``\| 'redeem'   \| 'NthPurchase'   \| 'merge-user'   \| 'SimpleCashbackRule'   \| 'BirthdateGift'   \| 'TajrobehAppreciation'   \| 'edit_order'   \| 'cancel_order'   \| 'edit_shipping_cost'   \| 'edit_cashback'   \| 'gift'   \| 'others'   \| 'Refund'   \| 'Charge'   \| 'Expired'   \| `${string}:activity` ``
{/* AUTO_FIELDS_END */}

